# Populated at build time from gradle/libs.versions.toml via processResources.
errorProneCore=${errorProneCoreVersion}
nullaway=${nullawayVersion}
googleJavaFormat=${googleJavaFormatVersion}
checkstyle=${checkstyleVersion}
