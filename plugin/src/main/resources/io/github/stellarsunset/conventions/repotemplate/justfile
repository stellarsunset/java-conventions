# Seeded by io.github.stellarsunset.java-conventions @pluginVersion@. Safe to edit; this file is
# written only when absent and is never overwritten once present.

# List all available tasks
default:
  just --list

# Verify the project (test, plus functionalTest/linters where they exist)
test:
  ./gradlew check

# Tag, push, and publish a new release (type = patch | minor | major)
release type='patch': test
  ./gradlew release -P{{type}}
  git push origin tag $(git describe --tags --abbrev=0)
  ./gradlew publish
